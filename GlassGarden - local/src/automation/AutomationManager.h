#pragma once

/*
------------------------------------------------------------
GlassGarden

File : AutomationManager.h

وظیفه:
تصمیم‌گیری خودکار برای کنترل تجهیزات
بر اساس دما، رطوبت و زمان‌بندی

همچنین شامل:
- Minimum ON/OFF Time برای فن، پمپ و مه‌ساز
  (جلوگیری از سوییچینگ سریع و آسیب به موتورها)
- Safe Mode : در صورت قطعی طولانی سنسور DHT،
  فن/پمپ/مه‌ساز به‌صورت خودکار خاموش نگه داشته
  می‌شوند تا تصمیم‌گیری بر اساس دادهٔ نامعتبر/قدیمی
  رخ ندهد. روشنایی از این قانون مستثناست و طبق
  زمان‌بندی عادی کار می‌کند.

هر دو محدودیت فقط روی تصمیمات این کلاس (حالت Auto)
اعمال می‌شوند؛ فرمان‌های دستی/Blynk تحت تأثیر قرار
نمی‌گیرند.

Version : 1.3.0
------------------------------------------------------------
*/

class AutomationManager
{
public:

    // راه‌اندازی
    void begin();

    // بروزرسانی
    void update();

private:

    //----------------------------------------------------
    // زمان آخرین تغییر وضعیت هر تجهیز (میلی‌ثانیه از بوت)
    // برای اعمال Minimum ON/OFF Time
    //----------------------------------------------------

    unsigned long fanLastChangeMs    = 0;
    unsigned long pumpLastChangeMs   = 0;
    unsigned long foggerLastChangeMs = 0;

    //----------------------------------------------------
    // بررسی این‌که آیا از آخرین تغییر وضعیت این تجهیز،
    // حداقل زمان لازم (MIN_STATE_DURATION_MS) گذشته است
    //----------------------------------------------------

    bool canChangeState(unsigned long lastChangeMs) const;

};

//============================================================
// Global Automation Manager
//============================================================

extern AutomationManager automation;